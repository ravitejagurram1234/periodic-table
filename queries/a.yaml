sg:
  openapi:
    sg-connect:
      implicit:
        scopes:
          "[api.quark.v1]": Authorization to use v1 of api.quark.v1 API
      client-credentials:
        token-url: ${SG_CONNECT_ROOT_URI}/access_token
        scopes:
          "[api.quark.v1]": >-
            Authorization to use v1 of quark-backend API
-DJPA_SHOW_SQL=false
